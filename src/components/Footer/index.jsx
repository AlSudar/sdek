const Footer = ({ width }) => {
  // width - возвращает текующую ширину экрана пользователя. Удобно, когда тебе в десктопе нужно собрать сетку одним образом, а в мобилке другим.
  console.log(width);
  return <footer></footer>;
};

export { Footer };
